# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# JetBrains IDE files
/.vscode

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
# Real secrets live in .env.production for Cloudflare/OpenNext prod builds
# (NEXT_PUBLIC_* and runtime keys). NEVER commit; treat as .env.local.
.env.production
.env.development

# vercel
.vercel

# typescript
*.tsbuildinfo

# cloudflare workers + opennext
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts

# project-specific
references/
.idea/
.claude/

# local backtest / scratch artifacts (not source)
artifacts/
.worktrees/
.data/
agent-2mo-result.json
run_result*.json
codex_review_request.md
.perry-cache/

# Tangent deploy helpers live locally in this project (needs Selbo's Circle
# SDK + DB + env) but are NOT part of Selbo's product. Run from disk; never
# commit. See sister repo D:/Projects/arc-perp-reference for the project.
scripts/tangent-deploy.ts
scripts/tangent-smoke-test.ts
app/services/tangent/

# emails: auto-generated by `bun run emails:build` (prebuild hook on CI)
lib/email/compiled-templates.ts
